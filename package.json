{
  "name": "sowmya-recipes",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "js/index.js",
  "scripts": {
    "start": "live-server",
    "build": "watch-run -d 100 -i -p \"html/**/*\" \"node build.js\"",
    "copy": "cpx \"src/**/*.{css,js,png,jpg}\" public --watch --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dheeraj.vs",
  "license": "MIT",
  "devDependencies": {
    "cpx": "^1.5.0",
    "handlebars": "^4.0.8",
    "live-server": "^1.2.0",
    "moment": "^2.18.1",
    "watch-run": "^1.2.5",
    "yamljs": "^0.2.10"
  },
  "dependencies": {
    "material-components-web": "^0.10.0"
  }
}
